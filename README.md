A full‑stack visual pipeline builder that lets users drag‑and‑drop nodes, connect them, and execute AI workflows in real time. The frontend uses React + ReactFlow with Zustand for state, and the backend uses FastAPI with graph validation and topological sorting. Supports Input, Text, LLM, and Output nodes and displays execution results in a clean UI.

